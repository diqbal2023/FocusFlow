# Releases

In-repo mirror of published GitHub Release assets for **FocusFlow v1.0**.

See `FocusFlow v1.0/` for the signed `FocusFlow.msix` plus installation instructions and release notes. Build outputs under `artifacts/` remain gitignored; only packages under `Releases/` are intended to be tracked.
